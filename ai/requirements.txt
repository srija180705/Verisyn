# GroqLLMProvider (ai/llm/provider.py) calls the Groq API directly over
# REST via httpx - already a backend dependency (backend/requirements.txt),
# shared single venv, nothing new to install.
httpx

# BedrockLLMProvider (ai/llm/provider.py) - official AWS SDK, only needed
# if AI_PROVIDER=bedrock is actually selected (imported lazily).
boto3
